package piscine

func ContSub(ch, subS, line string, index int) bool {
//take in the full line and look for where the substrings are
//see the ch given and if its the right ch of the substring using the index
return true
}
